### Trigger



- 触发器，附加在对象上的组件，监听场景中发生的事件并触发对应指令
- 上图，触发器监听键盘的空格键，按下后，调用对应指令“看起来是log”
- 添加至任意的Gameobject以完成对应的trigger组件添加

Events

- 一个可以在Tigger下跑的event

On Input Button

- 在Input -> On Input Button 下
- 当检测到按钮被按下是，触发这个Event事件
- 将作为全局作用，设置为“游戏对象”，只有在目标对象半径范围内才会触发
